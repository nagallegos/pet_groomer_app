{
  "private": true,
  "engines": {
    "node": ">=20.19.0"
  },
  "workspaces": [
    "react-app"
  ],
  "scripts": {
    "dev": "npx netlify dev --port 8888",
    "build": "npm run build --workspace react-app",
    "lint": "npm run lint --workspace react-app",
    "dev:web": "npm run dev --workspace react-app",
    "dev:web:lan": "npm run dev --workspace react-app -- --host 0.0.0.0 --port 3000",
    "dev:api": "npx netlify dev --port 8888",
    "netlify:dev": "npx netlify dev --port 8888",
    "seed:test-data": "node scripts/seed-test-data.mjs"
  },
  "dependencies": {
    "@netlify/neon": "^0.1.0",
    "bootstrap": "^5.3.8"
  },
  "devDependencies": {
    "@types/react-big-calendar": "^1.16.3"
  }
}
